package com.kafka.transform;

import pojo.destination.*;
import pojo.source.*;

public class PojoTransform {
	DestinationMessage dstMsg;
	SourceMessage srcMsg;
	Item item;
	String change= item.getChange();
	
	public void performTransform() {
		if(change == )
	}
		//if(change == ["DELETED,ORDERED,SHIPPED,DELIVERED"]){
					
	//}
}
